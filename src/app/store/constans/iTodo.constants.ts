export enum listActionTypes {
    LOAD = '[LISTS} LOAD',
    ADD = '[LISTS} ADD',
    DELETE = '[LISTS} DELETE',
    UPDATE = '[LISTS} UPDATE',
}

export enum todoActionTypes {
    LOAD = '[TODOS} LOAD',
    ADD = '[TODOS} ADD',
    DELETE = '[TODOS} DELETE',
    UPDATE = '[TODOS} UPDATE',
}
